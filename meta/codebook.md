# Codebook

The final machine-readable codebook is stored in `codebook_final.csv`.

Supporting files:

- `codebook_final_qa.csv` - completeness QA for descriptions, examples, and regex logic.
- `codebook_alignment_report.csv` - alignment between the prior Excel codebook and current YAML category set.
- `yaml_regex_audit.csv` - regex audit table exported from the current YAML system.
- `regex_refactor_notes.md` - conservative cleanup notes before PCA preparation.
