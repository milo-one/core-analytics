# Data fixtures

This folder contains only files that are explicitly allowed for repository use.

- `features_full.csv`: feature table generated from the repository test texts.

Raw text fixtures for the pipeline live in `../data_raw/`.
Generated pipeline output remains in `../out/` and is not versioned by default.
